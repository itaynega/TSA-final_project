diff --git a/models/TQNet.py b/models/TQNet.py
index 0176b87..099d900 100644
--- a/models/TQNet.py
+++ b/models/TQNet.py
@@ -14,8 +14,9 @@ class Model(nn.Module):
         self.dropout = configs.dropout
         self.use_revin = configs.use_revin
 
-        self.use_tq = True  # ablation parameter, default: True
-        self.channel_aggre = True   # ablation parameter, default: True
+        # Ablation switches. Defaults reproduce the published model exactly.
+        self.use_tq = bool(getattr(configs, 'use_tq', 1))
+        self.channel_aggre = bool(getattr(configs, 'channel_aggre', 1))
 
         if self.use_tq:
             self.temporalQuery = torch.nn.Parameter(torch.zeros(self.cycle_len, self.enc_in), requires_grad=True)
diff --git a/run.py b/run.py
index c163ad3..7546766 100644
--- a/run.py
+++ b/run.py
@@ -36,6 +36,10 @@ parser.add_argument('--pred_len', type=int, default=96, help='prediction sequenc
 parser.add_argument('--cycle', type=int, default=24, help='cycle length')
 parser.add_argument('--model_type', type=str, default='mlp', help='model type, options: [linear, mlp]')
 parser.add_argument('--use_revin', type=int, default=1, help='1: use revin or 0: no revin')
+parser.add_argument('--use_tq', type=int, default=1,
+                    help='TQNet ablation: 1 keep the Temporal Query, 0 replace it with self-attention')
+parser.add_argument('--channel_aggre', type=int, default=1,
+                    help='TQNet ablation: 1 keep the channel attention layer, 0 remove it')
 
 # PatchTST
 parser.add_argument('--fc_dropout', type=float, default=0.05, help='fully connected dropout')
@@ -120,12 +124,17 @@ print(args)
 
 Exp = Exp_Main
 
+# Empty for the published configuration, so reproduction runs keep their original
+# checkpoint paths and result.txt labels untouched.
+abl_tag = '' if (args.use_tq == 1 and args.channel_aggre == 1) \
+    else '_tq{}ca{}'.format(args.use_tq, args.channel_aggre)
+
 
 if args.is_training:
     for ii in range(args.itr):
 
         # setting record of experiments
-        setting = '{}_{}_{}_ft{}_sl{}_pl{}_cycle{}_seed{}'.format(
+        setting = '{}_{}_{}_ft{}_sl{}_pl{}_cycle{}_seed{}{}'.format(
             args.model_id,
             args.model,
             args.data,
@@ -133,7 +142,8 @@ if args.is_training:
             args.seq_len,
             args.pred_len,
             args.cycle,
-            fix_seed)
+            fix_seed,
+            abl_tag)
 
         exp = Exp(args)  # set experiments
         print('>>>>>>>start training : {}>>>>>>>>>>>>>>>>>>>>>>>>>>'.format(setting))
@@ -149,7 +159,7 @@ if args.is_training:
         torch.cuda.empty_cache()
 else:
     ii = 0
-    setting = '{}_{}_{}_ft{}_sl{}_pl{}_cycle{}_seed{}'.format(
+    setting = '{}_{}_{}_ft{}_sl{}_pl{}_cycle{}_seed{}{}'.format(
         args.model_id,
         args.model,
         args.data,
@@ -157,7 +167,8 @@ else:
         args.seq_len,
         args.pred_len,
         args.cycle,
-        fix_seed)
+        fix_seed,
+        abl_tag)
 
     exp = Exp(args)  # set experiments
     print('>>>>>>>testing : {}<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<'.format(setting))
