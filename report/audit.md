# Split and leakage audit -- ETTh1, L=96 -> H=96

Generated by `tools/audit_split.py`. Every number below is recomputed from the
CSV and from the fitted scaler, not read from documentation.

## Data

| Property | Value |
|---|---|
| File | `TQNet\dataset\ETTh1.csv` |
| sha256 | `f18de3ad269cef59bb07b5438d79bb3042d3be49bdeecf01c1cd6d29695ee066` |
| Rows in file | 17,420 |
| Rows used | 14,400 |
| Rows never used | 3,020 |
| Timestamp range | 2016-07-01 00:00:00 .. 2018-06-26 19:00:00 |
| Channels | HUFL, HULL, MUFL, MULL, LUFL, LULL, OT |
| Missing cells | 0 |
| Split fingerprint | `b66ee6b47e2b2eb8` |

The 3,020 discarded rows are the tail of the file. The paper's Table 1 lists
ETTh1 as 14,400 timesteps without mentioning that anything was dropped;
reproducing the paper means dropping them too.

## Split

| Split | CSV rows | Length | First target row | Windows |
|---|---|---|---|---|
| train | `[0, 8640)` | 8,640 | 96 | 8,449 |
| val | `[8544, 11520)` | 2,976 | 8,640 | 2,785 |
| test | `[11424, 14400)` | 2,976 | 11,520 | 2,785 |
| never used | `[14400, 17420)` | 3,020 | -- | -- |

## Fitted scaler

Read off the fitted object, then checked against an independent recomputation
of the training-rows statistics.

| Channel | mean | scale (sd) |
|---|---|---|
| HUFL | 7.937742 | 5.812749 |
| HULL | 2.021039 | 2.090105 |
| MUFL | 5.079771 | 5.518794 |
| MULL | 0.746186 | 1.926379 |
| LUFL | 2.781762 | 1.023523 |
| LULL | 0.788453 | 0.630237 |
| OT | 17.128262 | 9.176491 |

## Findings

| Stage | Ruling | Finding | Evidence |
|---|---|---|---|
| Grid integrity | **CLEAN** | strict hourly grid, no gaps, no missing cells | 17420 rows, all diffs == 1h, 0 NaN |
| Chronology | **CLEAN** | splits are chronological; target ranges do not overlap | targets train (96, 8640), val (8640, 11520), test (11520, 14400) |
| Scaler provenance | **CLEAN** | fitted statistics equal the training rows [0, 8640) and differ measurably from the whole-series statistics | max |train mean - full mean| = 3.8036 |
| Target disjointness | **CLEAN** | no row is a forecast target in more than one split | shared target rows: {'train/val': 0, 'train/test': 0, 'val/test': 0} |
| Input reach-back | **DISCLOSE** | validation and test inputs read up to 96 rows before their split boundary, which is upstream's design and is not leakage: those rows are inputs only, never targets, and never fitted on | first val window starts at row 8544, first val target at row 8640 |
| Model selection | **CLEAN** | early stopping and best-checkpoint are keyed on validation loss | exp_main.py: early_stopping(vali_loss, ...) |
| Observation hygiene | **DISCLOSE** | test loss is evaluated and printed every epoch alongside validation loss. It does not enter early stopping or checkpoint selection, so it is not leakage in the code -- but it is a human-in-the-loop channel and has to be disclosed | exp_main.py: test_loss = self.vali(test_data, test_loader, criterion) |
| Feature construction | **CLEAN** | the cycle index is a function of the row number alone (`(row + L) mod W`), so it cannot carry future values | checked on all 2785 test windows |
| Covariates | **CLEAN** | the loader computes calendar features but TQNet's forward signature takes only (x, cycle_index), so they are never consumed | TQNet.py: def forward(self, x, cycle_index): |
| Metric scale | **DISCLOSE** | de-normalisation is left commented out upstream, so MSE/MAE are in z-scored units, not degrees Celsius. This is why 0.3712 is the right order of magnitude, and why MAPE and SMAPE are unusable: the normalised series crosses zero in every channel | denorm lines commented: True; every test channel crosses zero: True |

`CLEAN` = checked and correct. `DISCLOSE` = not leakage, but the report must
state it. `FAIL` = blocks the reconstruction.

**Verdict: PASS**

No future information reaches training, preprocessing or feature
construction. The two `DISCLOSE` items are upstream's design, not ours,
and both are named in the report.
